#!/bin/bash
# =============================================================================
# SteamMachine-DIY - SteamOS Compatibility Wrapper
# =============================================================================
# Script: steamos-session-select
# Description: Compatibility link for Steam's internal "Return to Desktop" call.
# Redirects all logic to the central os-session-select manager.
# =============================================================================

# We pass "plasma" as the target because this script is typically called 
# by the Steam Deck UI when the user selects "Switch to Desktop".
exec /usr/local/bin/os-session-select "plasma"
